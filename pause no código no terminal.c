/* 
? Devido ao fato do script ser compilado ao ser executado, para pausarmos o script ao executar, temos que usar o comando:
*/

// * getchar()
#include <stdio.h>
int main(){
    printf("Estou pausando o código no terminal");
    getchar();
}